// Bài tập 07: Array (Mảng)
// Yêu cầu: Sử dụng các cú pháp khai báo mảng của TypeScript để hoàn thành các yêu cầu dưới đây.

// 1. Hãy khai báo một mảng `scores` chỉ chứa các số (number) sử dụng cú pháp `type[]`.
// Gán cho nó một mảng gồm 3 số bất kỳ.


// 2. Khai báo một mảng `fruits` chỉ chứa các chuỗi (string) sử dụng cú pháp Generic `Array<type>`.
// Gán giá trị mẫu cho mảng này.


// 3. Khai báo một mảng `mixedList` có thể chứa đồng thời cả giá trị kiểu `number` và kiểu `boolean`.
// Thử gán giá trị: [1, true, 2, false] xem có hợp lệ không.


// 4. Định nghĩa một interface `IProduct` gồm: `id` (number), `title` (string), `price` (number).
// Khai báo một mảng `cart` chứa các sản phẩm có cấu trúc `IProduct`.
// Gán mảng mẫu chứa ít nhất 2 sản phẩm.


// 5. Khai báo một mảng chỉ đọc `immutableScores` chứa các số điểm [10, 9, 8].
// Thử viết dòng code dùng phương thức `.push()` để thêm phần tử vào mảng này và quan sát lỗi của TS.
// (Comment dòng code lỗi đó lại và viết lời giải thích ngắn gọn vì sao bị lỗi).
