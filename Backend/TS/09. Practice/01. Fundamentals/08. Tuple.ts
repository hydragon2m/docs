// Bài tập 08: Tuple (Mảng cố định)
// Yêu cầu: Sử dụng các tính năng Tuple của TypeScript để giải quyết các bài toán dưới đây.

// 1. Hãy khai báo một biến `rgbColor` là một Tuple chứa 3 phần tử tương ứng với mã màu RGB.
// Cả 3 phần tử đều bắt buộc có kiểu `number`. Gán giá trị mẫu cho màu đỏ [255, 0, 0].


// 2. Sử dụng Named Tuple (Tuple có nhãn) để định nghĩa kiểu dữ liệu `HTTPResponse`.
// Kiểu này đại diện cho phản hồi API gồm:
// - statusCode (number)
// - statusMessage (string)
// Sau đó khai báo biến `response200` sử dụng kiểu này.


// 3. Khai báo một Tuple chỉ đọc (Readonly Tuple) tên là `readOnlyConfig` đại diện cho cài đặt ứng dụng:
// - host (string)
// - port (number)
// Thử dùng phương thức `.push()` hoặc gán đè phần tử để kiểm tra lỗi.
// (Comment dòng code lỗi lại và ghi giải thích ngắn gọn).
