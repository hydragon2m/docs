// Bài tập 04: Object Types (Kiểu đối tượng)
// Yêu cầu: Thực hiện khai báo kiểu dữ liệu đối tượng chính xác cho các trường hợp dưới đây.

// 1. Đối tượng lồng nhau (Nested Object)
// Hãy khai báo biến `employee` có kiểu dữ liệu phù hợp chứa:
// - id (number)
// - name (string)
// - address (đối tượng con chứa street: string, city: string)
// Và gán giá trị mẫu cho biến này.
let employee;


// 2. Thuộc tính động (Index Signatures)
// Hãy khai báo một đối tượng `phoneBook` biểu diễn danh bạ điện thoại động.
// Đối tượng này có thể có số lượng thuộc tính bất kỳ, nhưng key phải luôn là `string` (tên người) 
// và value phải luôn là `string` (số điện thoại).
let phoneBook;


// 3. Phân biệt kiểu `object` viết thường
// Hãy khai báo một biến `someData` với kiểu `object` (viết thường).
// Thử gán giá trị là một Object { key: 123 }, một Array [1, 2, 3], và một String "hello" xem cái nào bị báo lỗi.
// (Comment dòng bị lỗi lại kèm theo lời giải thích ngắn gọn).
let someData;
// test gán:
// someData = { key: 123 };
// someData = [1, 2, 3];
// someData = "hello";
