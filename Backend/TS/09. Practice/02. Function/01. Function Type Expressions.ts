// Bài tập 01: Function Type Expressions
// Yêu cầu: Khai báo kiểu cho các biến và hàm sử dụng Function Type Expressions.

// 1. Khai báo một biến `mathOp` có kiểu dữ liệu là một hàm nhận vào 2 tham số:
// x (number), y (number) và trả về một number.
// Gán một hàm thực hiện phép nhân cho biến này.


// 2. Viết hàm `processUsers` nhận vào một danh sách tên `names` (mảng string[])
// và một hàm callback `logger` (hàm nhận vào `string` và không trả về gì).
// Hàm `processUsers` sẽ duyệt qua từng tên trong mảng và gọi hàm callback `logger` truyền tên đó vào.
function processUsers(names, logger) {
  // Viết logic tại đây
}
