// Bài tập 04: Optional & Default Parameters
// Yêu cầu: Định nghĩa hàm sử dụng tham số tùy chọn và mặc định.

// 1. Tham số tùy chọn (Optional)
// Viết hàm `buildName` nhận vào `firstName` (string) và `middleName` (string - optional), và `lastName` (string).
// (Chú ý quy tắc thứ tự sắp xếp tham số của TypeScript).
// Trả về tên đầy đủ dạng: "firstName middleName lastName" hoặc "firstName lastName".


// 2. Tham số mặc định (Default)
// Viết hàm `applyDiscount` nhận vào `price` (number) và `discountPercent` (number, mặc định là 10).
// Trả về giá tiền sau khi đã giảm.
