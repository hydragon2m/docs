// Bài tập 05: Function Overloads (Nạp chồng hàm)
// Yêu cầu: Triển khai nạp chồng hàm theo thiết kế chữ ký kiểu.

// Thiết kế một hàm `combine` có khả năng:
// 1. Nhận vào hai chuỗi `string` và trả về một chuỗi `string` (hành vi: nối chuỗi).
// 2. Nhận vào hai số `number` và trả về một số `number` (hành vi: cộng số).
//
// Hãy viết:
// - Các Overload Signatures.
// - Implementation Signature và thân hàm thực tế giải quyết logic trên.

// Viết chữ ký nạp chồng tại đây:


// Viết chữ ký triển khai tại đây:
function combine(a, b) {
  // Logic xử lý
}
